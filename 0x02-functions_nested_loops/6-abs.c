#include "main.h"

/**
 * _abs -  compute the absolute value of an interger
 * @c: integer to be computed
 * Return: Absollute value of number or zero
 */

int _abs(int c)
{
	if (c < 0)
	{
	int abs_val;

	abs _val = c * -1;
	return (abs_val);
	}
	return (c);
}
